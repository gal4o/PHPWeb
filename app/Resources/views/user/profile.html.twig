{% extends 'base.html.twig' %}

{% block body_id %}

{% endblock %}

{% block main %}
    <div class="container body-content">
        <div class="row">
            <header>
                <h1>It is {{ user.fullName }} profile!</h1>
            </header>

            <footer>
                <a class="btn btn-default btn-xs" href="{{ path('homepage') }}">Home</a>
                <a class="btn btn-success btn-xs" href="{{ path('user_edit',{'id':user.id}) }}">Edit</a>
                <a class="btn btn-danger btn-xs" href="{{ path('user_delete',{'id':user.id}) }}">Delete</a>
            </footer>
        </div>
    </div>
{% endblock %}

